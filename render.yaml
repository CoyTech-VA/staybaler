services:
  - type: web
    name: staybaler
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm start
    healthCheckPath: /api/rooms
    envVars:
      - key: NODE_VERSION
        value: 24.18.0
